// Public rendering receives only shapeStorefrontItem's allowlisted fields.
const origin = 'https://themanapocket.com';
export const escapeHtml = v => String(v ?? '').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const json = v => JSON.stringify(v).replace(/</g,'\\u003c');
const money = v => '$'+Number(v||0).toFixed(2);
export const isBcwItem = i => i.dropship === true && String(i.vendor).toUpperCase() === 'BCW';
export const productPath = i => '/item/'+encodeURIComponent(i.id)+'/'+(String(i.name||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'card');
export function isBcwPublished(i){
  return isBcwItem(i) && bcwAvailable(i) && i.onlineListed && i.price>0 && !i.soldAt && !i.archivedAt && !i.linkUrl && !['sold','archived','returned','deleted','sold_pending_pickup','sold_pending_shipment','hold','lost_damaged','presale','bundled'].includes(i.inventoryStatus);
}
export const bcwAvailable = i => i.quantity>0 && i.supplierAvailability==='in_stock';
export function catalogSelection(items, params){
  const q=(params.get('q')||'').trim().slice(0,120), category=(params.get('category')||'').slice(0,300);
  const published=items.filter(isBcwPublished).sort((a,b)=>a.name.localeCompare(b.name));
  const categories=[...new Set(published.flatMap(i=>i.supplierCategoryPaths||[]))].sort();
  const selected=published.filter(i=>(!category||(i.supplierCategoryPaths||[]).includes(category))&&(!q||[i.name,i.supplierSku,i.description].join(' ').toLowerCase().includes(q.toLowerCase())));
  const pages=Math.max(1,Math.ceil(selected.length/36));
  const page=Math.max(1,Math.floor(Number(params.get('page'))||1));
  return {q,category,categories,page,pages,total:selected.length,items:selected.slice((page-1)*36,page*36)};
}
function shell({title,description,path,body,schemas=[],image='',noindex=false}){
 return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)} | The Mana Pocket</title><meta name="description" content="${escapeHtml(description)}"><link rel="canonical" href="${origin}${escapeHtml(path)}"><meta name="robots" content="${noindex?'noindex,follow':'index,follow'}"><meta property="og:title" content="${escapeHtml(title)}"><meta property="og:description" content="${escapeHtml(description)}"><meta property="og:url" content="${origin}${escapeHtml(path)}"><meta property="og:type" content="website">${image?`<meta property="og:image" content="${escapeHtml(image)}">`:''}<script type="application/ld+json">${json(schemas)}</script><style>
 :root{color-scheme:dark;font-family:Inter,Arial,sans-serif;background:#0c0d13;color:#f0f0f6}*{box-sizing:border-box}body{margin:0}a{color:inherit;text-decoration:none}a:hover{text-decoration:underline}button,input,select{font:inherit}button,.button{cursor:pointer;background:#bdff69;color:#111519;border:0;border-radius:8px;padding:13px 20px;font-weight:700;display:inline-block}button:disabled{opacity:.5;cursor:default}header,footer{border-bottom:1px solid #2d3039;padding:22px max(5vw,20px)}header{display:flex;align-items:center;justify-content:space-between;gap:20px;flex-wrap:wrap}nav{display:flex;gap:22px;align-items:center;flex-wrap:wrap}.brand{font-size:20px;font-weight:900;letter-spacing:.04em}.brand span,.eyebrow{color:#bdff69}.wrap{max-width:1280px;margin:auto;padding:45px 24px}h1{font-size:clamp(32px,5vw,58px);letter-spacing:-.04em;margin:14px 0 18px}h2{font-size:18px;line-height:1.4}p{line-height:1.7}.muted{color:#a9aabb}.intro{max-width:780px}.eyebrow{font-size:12px;letter-spacing:.15em;text-transform:uppercase}.filters{display:flex;gap:12px;flex-wrap:wrap;margin:32px 0 24px}.filters label{display:grid;gap:7px;font-size:12px;flex:1;min-width:180px}.filters input,.filters select{background:#191b25;color:#f0f0f6;border:1px solid #383b49;border-radius:8px;padding:13px;width:100%}.filters button{align-self:end}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(235px,1fr));gap:20px}.card{background:#171923;border:1px solid #2d303c;border-radius:14px;overflow:hidden}.card .copy{padding:20px}.card img{background:white;object-fit:contain;width:100%;height:230px;padding:15px}.price{font-size:24px;font-weight:700;color:#bdff69}.sku{font-size:12px;color:#aaaec0;overflow-wrap:anywhere}.pagination{display:flex;justify-content:space-between;align-items:center;margin:30px 0}.detail{display:grid;grid-template-columns:1fr 1fr;gap:48px;margin-top:28px}.gallery img{width:100%;background:#fff;object-fit:contain;max-height:550px;border-radius:14px}.gallery .thumbs{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:12px}.thumbs img{height:100px}.description{white-space:pre-line}.specs{width:100%;border-collapse:collapse}.specs th,.specs td{text-align:left;padding:12px;border-bottom:1px solid #2d3039;overflow-wrap:anywhere}.empty{padding:60px 24px;background:#171923;border-radius:14px;text-align:center}footer{border-top:1px solid #2d3039;border-bottom:0;margin-top:50px;color:#a9aabb}footer nav{margin-top:20px}a:focus-visible,button:focus-visible{outline:3px solid #bc92ff;outline-offset:4px}@media(max-width:700px){.detail{grid-template-columns:1fr;gap:22px}.wrap{padding:30px 18px}.grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card img{height:170px}.card .copy{padding:12px}.card h2{font-size:15px}nav{gap:14px}}
 </style><script defer src="https://wo-checkout.swarnerauto.workers.dev/wo-cart.js"></script></head><body><header><a class="brand" href="/">THE MANA <span>POCKET</span></a><nav aria-label="Main navigation"><a href="/shop">Shop</a><a href="/bcw">BCW Supplies</a><a href="/preorder">Comic preorders</a><button type="button" id="bcw-cart">Cart <span id="wo-cart-badge" class="wo-cart-badge"></span></button></nav></header><main class="wrap">${body}</main><footer><strong>The Mana Pocket · Collector supplies</strong><p>Protect, organize, and display your collection. BCW products ship directly from our supplier.</p><nav aria-label="Footer navigation"><a href="/">Home</a><a href="/shop">Shop all</a><a href="/bcw">BCW supplies</a><a href="/sitemap-items.xml">Product sitemap</a></nav></footer><script>document.addEventListener('DOMContentLoaded',function(){document.getElementById('bcw-cart').addEventListener('click',function(){if(window.WO&&WO.openCart)WO.openCart();else location.href='/shop';});document.querySelectorAll('[data-bcw-product]').forEach(function(b){b.addEventListener('click',function(){if(!window.WO||!WO.addToCart){location.href='/shop?item='+encodeURIComponent(b.dataset.itemId);return;}WO.addToCart(JSON.parse(b.dataset.bcwProduct),b);b.textContent='Added to cart';});});});</script></body></html>`;
}
export function renderBcwCatalog(selection){
 const {q,category,categories,page,pages,total,items}=selection;
 const href=n=>{const p=new URLSearchParams();if(category)p.set('category',category);if(q)p.set('q',q);if(n>1)p.set('page',n);return '/bcw'+(p.size?'?'+p:'');};
 const title=category?`${category} — BCW supplies`:'BCW collector supplies';
 const description='Shop BCW comic bags, boards, card sleeves, storage boxes, and display supplies at The Mana Pocket. Find the right protection for your collection.';
 const body=`<div class="eyebrow">Protect · Organize · Display</div><h1>${escapeHtml(title)}</h1><p class="intro muted">Give your collection a home. Browse our BCW supplies for comics, trading cards, and collectibles, shipped directly from our supplier.</p><form class="filters" action="/bcw"><label>Find a product or BCW SKU<input name="q" type="search" value="${escapeHtml(q)}" placeholder="Comic bags, sleeves, storage…"></label><label>Category<select name="category"><option value="">All categories</option>${categories.map(c=>`<option value="${escapeHtml(c)}"${c===category?' selected':''}>${escapeHtml(c)}</option>`).join('')}</select></label><button>Search supplies</button></form><p class="muted">${total} product${total===1?'':'s'}${total?` · Page ${page} of ${pages}`:''}</p>${items.length?`<div class="grid">${items.map(i=>`<article class="card"><a href="${escapeHtml(productPath(i))}">${i.image?`<img src="${escapeHtml(i.image)}" alt="${escapeHtml(i.name)}" loading="lazy">`:''}<div class="copy"><div class="sku">BCW · ${escapeHtml(i.supplierSku||'')}</div><h2>${escapeHtml(i.name)}</h2><p class="muted">${escapeHtml(i.supplierSellingUnit||'')}</p><div class="price">${money(i.price)}</div><p class="sku">${bcwAvailable(i)?'Ships from supplier':'Currently unavailable'}</p></div></a></article>`).join('')}</div>`:`<div class="empty"><h2>${q||category?'No matching supplies':'BCW supplies are coming soon'}</h2><p>${q||category?'Try a different name or SKU, or browse all categories.':'We’re preparing our selection of collector supplies. Explore the shop while we get the shelves ready.'}</p><a class="button" href="${q||category?'/bcw':'/shop'}">${q||category?'Browse all supplies':'Explore the shop'}</a></div>`}<nav class="pagination" aria-label="Catalog pages">${page>1?`<a href="${escapeHtml(href(page-1))}" rel="prev">← Previous</a>`:'<span></span>'}${page<pages?`<a href="${escapeHtml(href(page+1))}" rel="next">Next →</a>`:''}</nav>`;
 return shell({title,description,path:href(page),body,noindex:!!q||!total,schemas:[{'@context':'https://schema.org','@type':'CollectionPage',name:title,url:origin+href(page),mainEntity:{'@type':'ItemList',itemListElement:items.map((i,n)=>({'@type':'ListItem',position:(page-1)*36+n+1,url:origin+productPath(i),name:i.name}))}}]});
}
export function renderBcwProduct(item, canonicalPath=productPath(item)){
 const available=bcwAvailable(item), description=item.description||`${item.name} from BCW. Shop collector supplies at The Mana Pocket.`, photos=item.photos?.length?item.photos:[item.image].filter(Boolean);
 const cart={id:item.id,name:item.name,price:item.price,image:item.image||'',available:item.quantity,dropship:true};
 const body=`<nav aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><a href="/bcw">BCW supplies</a></nav><div class="detail"><div class="gallery">${photos[0]?`<img src="${escapeHtml(photos[0])}" alt="${escapeHtml(item.name)}">`:''}<div class="thumbs">${photos.slice(1).map((p,n)=>`<a href="${escapeHtml(p)}"><img src="${escapeHtml(p)}" alt="${escapeHtml(item.name)} — view ${n+2}" loading="lazy"></a>`).join('')}</div></div><div><div class="eyebrow">BCW collector supplies</div><h1>${escapeHtml(item.name)}</h1><p class="sku">BCW SKU: ${escapeHtml(item.supplierSku||'')}</p><p>${escapeHtml(item.supplierSellingUnit||'')}</p><p class="price">${money(item.price)}</p><p>${available?'Available · Ships directly from our supplier':'Currently unavailable'}</p><button type="button" data-item-id="${escapeHtml(item.id)}" data-bcw-product="${escapeHtml(json(cart))}" ${available?'':'disabled'}>${available?'Add to cart':'Out of stock'}</button><p class="muted">Supplier-fulfilled item. May ship separately from other items in your order.</p><h2>About this product</h2><p class="description">${escapeHtml(description)}</p>${Object.keys(item.supplierSpecifications||{}).length?`<h2>Product details</h2><table class="specs">${Object.entries(item.supplierSpecifications).map(([k,v])=>`<tr><th scope="row">${escapeHtml(k)}</th><td>${escapeHtml(v)}</td></tr>`).join('')}</table>`:''}</div></div>`;
 return shell({title:item.name+' — BCW',description:description.replace(/\s+/g,' ').slice(0,160),path:canonicalPath,body,image:item.image,schemas:[{'@context':'https://schema.org','@type':'Product',name:item.name,description,image:photos,sku:item.supplierSku||item.id,brand:{'@type':'Brand',name:'BCW'},...(/^\d{12}$/.test(item.supplierUpc||'')?{gtin12:item.supplierUpc}:{}),offers:{'@type':'Offer',url:origin+canonicalPath,priceCurrency:'USD',price:item.price,availability:'https://schema.org/'+(available?'InStock':'OutOfStock'),seller:{'@type':'Organization',name:'The Mana Pocket'}}},{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:origin+'/'},{'@type':'ListItem',position:2,name:'BCW supplies',item:origin+'/bcw'},{'@type':'ListItem',position:3,name:item.name,item:origin+canonicalPath}]}]});
}
